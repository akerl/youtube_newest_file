name 'slack_newest_file'
org 'akerl'

source(
  type: 'git',
  path: '.'
)

build do
  run 'make local'
  cp 'bin'
end

package(
  type: 'file',
  artifacts: [
    {
      source: 'bin/speculate_darwin',
      name: 'speculate_darwin'
    },
    {
      source: 'bin/speculate_linux',
      name: 'speculate_linux'
    },
    {
      source: 'bin/speculate_windows',
      name: 'speculate_windows'
    }
  ]
)

test do
  # TODO: add tests
end
